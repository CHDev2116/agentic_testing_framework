# Dependency version pins live only in pyproject.toml ([project.dependencies]).
# This file is a convenience shim for `pip install -r requirements.txt`.
# Equivalent: pip install -e ".[dev]"
-e .[dev]
